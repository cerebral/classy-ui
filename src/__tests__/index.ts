import { config } from '../config/base.config';

/*
import { transform as transformCss } from '../config/transform-css';
import { transform as transformTypes } from '../config/transform-types';

describe('TRANSFORM', () => {
  test('should transform to css', () => {
    const transformed = transformCss(config);

    console.log(transformed);
  });
  test('should transform to types', () => {
    const transformedCss = transformCss(config);
    const transformed = transformTypes(transformedCss);

    console.log(transformed);
  });
});
*/
